# 使用 Java 25 與 Spring Boot 4

RushBook 使用 Java 25 LTS、目前穩定的 Spring Boot 4.1、Gradle wrapper 與
Kotlin build scripts。這個專案沒有 legacy compatibility constraint，因此
新 codebase 直接採用目前的 LTS Java baseline 與穩定 Spring generation，
而不是從上一代 LTS stack 開始。
